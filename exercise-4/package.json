{
  "name": "exercise-4",
  "version": "1.0.0",
  "description": "You sure have noticed that the developer experience (#DX) until now is not being awesome enough. Let's try to improve it with some arrangements to your environment.",
  "main": "build/bundle.js",
  "scripts": {
    "start": "webpack-dev-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-webpack-plugin": "^1.6.1"
  }
}
